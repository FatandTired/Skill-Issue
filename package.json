{
  "devDependencies": {
    "turbo": "^2.5.5"
  },
  "packageManager": "npm@10.8.2",
  "name": "portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "workspaces": [
    "api",
    "client"
  ],
  "dependencies": {
    "@nestjs/config": "^4.0.2",
    "turbo-windows-64": "^2.5.5"
  },
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run build && cd api/dist && node main.js",
    "dev": "turbo run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": "22.x"
  }
}
